# ExamForge AI v0.4.1

## 修复

- 修复 DeepSeek 等模型在长材料场景中容易返回 `CONTEXT_TOO_LONG` 后直接退回本地安全底稿的问题。
- 新增长材料自动分块、chunk insight、紧凑 evidence pack 和最终合成重试。
- 修复 `.txt` 文件上传后可通过安全白名单但解析器不支持的问题。
- 修复前端默认 API 地址在云端和桌面版中不够稳的问题，生产构建默认使用同源 `/api`。

## 改进

- 增加可配置的 LLM 上下文预算、chunk 大小、chunk overlap、每轮 chunk 数、repair 次数和分块开关。
- DeepSeek 返回上下文过长时会自动压缩重试，只有多次失败后才回退本地安全底稿。
- 分块处理优先保留往年题题干、公式、定义、题型线索、代码片段、易错点和关键上下文。
- 前端在长材料分块成功时显示“分块 AI 深度整理”，失败时提供更清晰的重试建议。
- README 恢复为 UTF-8 中文，并补充网页端、桌面版、Docker、Render、Key 模式、隐私和免责声明。
- 更新云端部署文档、用户指南、Windows 桌面版说明和发布检查清单。

## 注意

- 本地整理模式无需 API Key，但 AI 深度整理建议配置 DeepSeek / OpenAI-compatible 模型。
- 云端部署请使用 HTTPS，并谨慎配置服务端默认 API Key、上传限制和成本监控。
- 请勿上传敏感资料、无授权资料或真实考试泄露内容。
