# Configuration

The app works without API keys by using rule-based analysis.

Runtime API keys can be entered in the web UI for the current request only. They are not stored in localStorage, not written to `.env`, and not returned by the backend.

Use `.env.example` as a placeholder reference only. Never put real keys in docs or commits.
