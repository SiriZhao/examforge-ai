# RecallForge v3.1 UX 改进报告

## 升级目标

本次升级面向没有技术背景的学生，降低首次使用、项目查找和异常恢复的理解成本。升级保持 React + Vite + TypeScript 技术栈，并保留文件上传、AI 生成和结果导出流程。

## 修改内容

### 1. 首次启动引导

- 新增三步 Welcome Flow，依次说明“上传文件 → AI 理解内容 → 生成复习资料”。
- 每一步提供清晰的进度、图标、说明、上一步和下一步操作。
- 用户可以选择“稍后再说”，完成或跳过后会在本地记录，后续启动不再重复打扰。
- 首次引导状态使用 `recallforge-welcome-complete-v3.1` 持久化。

### 2. 文件与项目管理

- 将项目按使用频率拆分为“最近项目”和“历史记录”，最近创建的 3 个项目优先展示。
- 项目条目显示文件名称、创建时间和生成状态。
- 新增明确的“重新打开”操作，可继续未完成项目或查看已有结果。
- 新增带二次确认的永久删除操作，避免误删。
- AI 生成进行中时禁用删除，避免后台任务和文件状态不一致。
- 后端新增 `DELETE /api/review-projects/projects/{project_id}`，删除项目前校验工作空间归属，并清理项目、报告、模块、版本和上传文件。

### 3. 耗时操作反馈

- 上传、诊断、生成、项目操作和工作空间连接均提供 Loading 状态。
- 全局耗时操作使用不确定进度提示；AI 生成继续使用服务端返回的真实阶段进度。
- 文件处理页明确展示上传、OCR、章节理解、AI 生成和完成阶段。
- 操作进行时禁用冲突按钮，减少重复提交。
- 下载、上传、诊断和生成失败后提供可执行的重试入口。

### 4. 错误处理与恢复

- 建立统一错误翻译，将网络/API 失败、文件格式错误、OCR 失败、模型不可用或额度不足、内容缺失和下载失败转换为学生可理解的说明。
- 错误提示同时说明下一步操作，例如重试、检查文件、切换模型或重新连接。
- 根节点新增 React Error Boundary。发生未捕获的渲染错误时显示恢复页面和重新加载按钮，避免白屏。
- 页面不再向用户展示模糊的“未知错误”或原始技术异常。

## 新增及升级组件

- `WelcomeFlow`：首次启动三步引导。
- `ErrorBoundary`：应用级白屏保护与恢复入口。
- `ProjectList`：最近项目、历史记录、重新打开和删除交互。
- `Progress`：全局不确定进度与生成阶段进度。
- `Button`：增加危险操作样式，用于删除确认。
- `Modal`：承载 Welcome Flow 和删除确认。
- `FileItem`：统一文件名称、时间、状态和操作布局。
- `useReviewWorkspace`：集中管理项目加载、删除、重试和面向用户的错误状态。
- `workspace API`：补充项目删除请求和统一 API 错误处理。

## 验证结果

验证日期：2026-09-09

| 验证项 | 结果 |
| --- | --- |
| 前端组件与流程测试 | 16/16 通过 |
| TypeScript 类型检查 | 通过（包含在生产构建中） |
| Vite 生产构建 | 通过，111 个模块完成转换 |
| 后端平台回归测试 | 5/5 通过 |
| Git 差异格式检查 | 通过，无空白字符错误 |
| Playwright 真实浏览器验收 | 通过 |

浏览器验收覆盖 1440 × 960 桌面端与 390 × 844 移动端：三步引导内容和操作正确，完成状态可持久化，刷新后不重复展示；两个视口均无横向溢出，控制台无错误，页面无未捕获异常。验收截图保存在 output/playwright/。

后端测试输出包含 FastAPI 依赖的弃用警告，不影响本次功能和测试结果。

## 后续优化建议

1. 增加可撤销删除或短期回收站，进一步降低误删风险。
2. 为长时间 OCR 和生成任务补充预计等待时间；需要后端提供稳定的耗时统计后再实现。
3. 在真实课程资料样本上持续验证错误文案，按失败原因和用户重试成功率调整提示优先级。
4. 增加持久化任务恢复，使用户关闭应用后仍能直接回到正在处理的阶段。
